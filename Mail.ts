export class Direcciones {
    tipo: string;
    direccion: string;
    
    // contructor

    constructor(tipo: string, direccion: string) {
    this.tipoCorreo = tipoCorreo;
    this.direccionCorreo = direccion;
    }

}
    // almacenar datos del codigo HTML
    let mailsUsuario = document.getElementById("guardar").addEventListener("click", () => {
        // Obtener los valores añadido por el usario y guardarlos en un array
        let tipoCorreo= document.getElementById("tipoCorreo");
        let direccionCorreo = document.getElementById("correo");
     });

